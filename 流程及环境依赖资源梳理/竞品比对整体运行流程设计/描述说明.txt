南京加工主站定时拉取可疑报警消息的结构化数据,整理传入给脚本的参数,
调用竞品机暴露出来的录制回放系统的取数脚本调用接口,一种是接口直接返回对应的竞品数据
另一种是导出文件到指定目录,然后定时同步到72机器的ftp,每隔十五分钟好像
加工主站通过接口返回对应的竞品数据,或者是轮询72机器ftp中相关竞品文件的数据

然后调用比对Agent,传入可疑消息以及竞品文件(数据),然后先通过AI对齐两边字段释义,
然后通过强逻辑和AI语义的区分规则,对比对路径进行工程化比对和AI比对的路径划分,然后输出比对结果

南京加工主站根据报警结果来做后续的程序反馈


质检相关系统运行在南京加工主站
啄木鸟主站可疑消息(1.自动发反馈为否 2.主题不含运行异常 3.主题不含正常)发送完后,
发到消息处理的kafka队列(主要处理通用代码转换流程,消息列表数据提取,整理发送给竞品机的入参，然后调用竞品机任务接口A)

方式一: 接口A直接返回<操作脚本所取回的数据>,但是要等
方式二: 接口A不返回<操作脚本所取回的数据>, <操作脚本所取回的数据>所取回的数据由ftp等形式传回主站

定时任务拉取ftp的对应取数文件和消息的结构化信息传入数据比对Agent进行数据比对然后二次复核



