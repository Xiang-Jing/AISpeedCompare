我现在要做一个事情，就是要开发录制和回放系统，意思就是，开启录制之后，人对电脑股票软件桌面客户端如同花顺完成一系列操作，
比如鼠标点击，键盘输入，等，结束录制之后，可以生成一个脚本，生成的脚本需要支持结构化参数传入(如果框架不支持，
暂时可以通过手动进行修改，修改成可换参数的脚本，或者通过一些程序进行后续的增强)，
就是比如录制的时候有一步骤输入了一个代码688697，然后回车，这时候就需要支持多个代码然后回车的这批操作，
有一步进行了，简单的是可以一个流程，多次执行，但是里面的代码这种，需要结构化出来，能够接收一个结构化的入参，
然后去遍历执行回放生成的脚本进行操作电脑，，，你觉得有没有这样的开源框架或者软件，是关于录制回放这种，
能够实现我这种业务需求的，please